# d-term-mod
hipda的python客户端，修改版。

某个坛友做的，之前在github好像有，后来找不到了。

后来论坛好像改版了，之前的版本用不了了，我稍微修改了一下，顺便改了一下配色，以适合linux的终端下使用。

域名换成4d4y，登录规则也变了，重新弄了一下，加入图片显示，运行的时候加参数-p，调用ueberzugpp在终端下显示图片，有图片的帖子上按tab显示缩略图，按enter显示大图。
